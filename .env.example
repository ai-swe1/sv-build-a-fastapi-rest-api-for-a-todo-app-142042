# Example environment configuration for the FastAPI Todo app
# -------------------------------------------------------------
# Port on which the application will be exposed
PORT=8000

# SQLite database URL (relative path works for Docker and local dev)
# Example: sqlite:///./todos.db
DATABASE_URL=sqlite:///./todos.db
